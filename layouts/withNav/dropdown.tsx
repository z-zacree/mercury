import { Box } from "@chakra-ui/react";
import { FC } from "react";

const Dropdown: FC = () => {
  return (
    <Box mt={4}>
      <span>Dropdown</span>
    </Box>
  );
};

export default Dropdown;
